project('com.app.lesson.egg.app-clock','vala','c')

executable(
    meson.project_name(),
    'eggclock.vala',
    dependencies:[
        dependency('gtk+-3.0'),
    ],
    link_args:['-X', '-lm'],
    install:true
)
